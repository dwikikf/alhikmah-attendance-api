$kind: http-request
name: Update Attendance
method: PUT
url: '{{base_url}}/api/v1/attendances/{{attendance_id}}'
order: 5000
headers:
  - key: Authorization
    value: 'Bearer {{auth_token}}'
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "status": "hadir",
      "notes": "Updated manually"
    }
