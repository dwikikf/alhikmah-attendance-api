$kind: http-request
name: Create Class
method: POST
url: "{{base_url}}/api/v1/classes"
order: 3000
headers:
  Authorization: Bearer {{auth_token}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "class_name": "Kelas Dwiki",
      "teacher_id": "{{user_id}}",
      "academic_year": "2026/2027",
      "capacity": 40,
      "description": ""
    }
