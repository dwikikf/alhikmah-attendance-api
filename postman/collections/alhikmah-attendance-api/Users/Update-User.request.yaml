$kind: http-request
name: Update User
method: PUT
url: "{{base_url}}/api/v1/users/{{user_id}}"
order: 4000
headers:
  Authorization: Bearer {{auth_token}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "full_name": "Dwiki",
      "email": "dwikikf@alhikmah.sch.id",
      "password": ""
    }
