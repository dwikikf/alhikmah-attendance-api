$kind: http-request
name: Create User
method: POST
url: "{{base_url}}/api/v1/users"
order: 3000
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "username": "super_admin",
      "email": "super_admin@alhikmah.sch.id",
      "password": "super_admin",
      "full_name": "Dwiki Kautsar Fahmi",
      "role": "admin"
    }
