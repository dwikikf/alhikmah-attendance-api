$kind: http-request
name: Login
method: POST
url: "{{base_url}}/api/v1/auth/login"
order: 1000
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "username": "superadmin",
      "password": "superadmin"
    }
